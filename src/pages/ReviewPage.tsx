import { PlaceholderScreen } from "@/components/placeholder-screen";
export default function ReviewPage() {
  return <PlaceholderScreen title="Review queue" intent="Re-screen and triage blocked / potential-match items. Wires to /api/compliance-screenings/screen-* and PUT /api/compliance-screenings/{id} for false-positive marking." />;
}
