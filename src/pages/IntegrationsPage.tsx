import { PlaceholderScreen } from "@/components/placeholder-screen";
export default function IntegrationsPage() {
  return <PlaceholderScreen title="Integrations" intent="Drive atomic-fi-agent / platform-agent from an NL textarea. Manage tenants and API keys via /api/tenants and /api/api-keys." />;
}
