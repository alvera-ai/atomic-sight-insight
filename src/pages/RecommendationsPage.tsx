import { PlaceholderScreen } from "@/components/placeholder-screen";
export default function RecommendationsPage() {
  return <PlaceholderScreen title="Recommendations" intent="Approve recommendations from the Alvera platform — each calls the matching AtomicFi write (POST /api/blocklist-entries, PUT /api/risk-classifications/{id}, …)." />;
}
