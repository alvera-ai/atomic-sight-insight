import { PlaceholderScreen } from "@/components/placeholder-screen";
export default function HealthPage() {
  return <PlaceholderScreen title="Health" intent="GET /api/info for version + DB status. RPS, p95, queue depth render from in-memory mock and are marked 'preview' until a metrics endpoint exists." />;
}
