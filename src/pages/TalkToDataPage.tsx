import { PlaceholderScreen } from "@/components/placeholder-screen";
export default function TalkToDataPage() {
  return <PlaceholderScreen title="Talk to data" intent="Cross-table NL queries powered by the same Copilot tools (search_tables → get_schema → get_related_tables → execute_query). Use the Copilot button bottom-right to try it now." />;
}
